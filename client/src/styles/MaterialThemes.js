import { createMuiTheme } from '@material-ui/core/styles';

// Material UI Theme
export const MaterialTheme = createMuiTheme({
  palette: {
    primary: { main: '#7f9eb2' },
    tertiary: { main: '#2196f3' }
  },
  typography: {
    fontSize: 20
  }
});
