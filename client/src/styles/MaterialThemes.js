import { createMuiTheme } from '@material-ui/core/styles';

// Material UI Theme
export const MaterialTheme = createMuiTheme({
  palette: {
    primary: { main: '#7f9eb2' }
  },
  typography: {
    fontSize: 20
  }
});
